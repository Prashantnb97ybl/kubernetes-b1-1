apiVersion: v1
kind: pod
metadata:
  name: cb-bs-pod
  labels:
    env: uat-env
    app: cb-bs-pod
   namespace: prod 
spec:
 containers: 
    name: cb-bs-instance
    image:nginx
    ports:
        name:http
        containerPort: 80


